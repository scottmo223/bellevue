backend:
  name: git-gateway
  repo: scottmo223/bellevue

publish_mode: editorial_workflow
media_folder: /content/assets

collections:
  - name: blog
    label: Blog
    folder: blog
    create: true
    fields:
      - { name: path, label: Path }
      - { name: date, label: Date, widget: date }
      - { name: title, label: Title }
      - { name: body, label: Body, widget: markdown }